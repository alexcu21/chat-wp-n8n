# ⚡ Quick Start - Comandos Rápidos

## 🚀 Instalación Inicial

```bash
npm install
npm run build
```

## 💻 Desarrollo

```bash
# Modo desarrollo con hot-reload
npm run dev

# Construir y ver cambios automáticamente
npm run watch
```

## 📦 Producción

```bash
# Build optimizado
npm run build

# Preview del build
npm run preview
```

## 🛠️ Utilidades

```bash
# Linting
npm run lint:js
npm run lint:css

# Formateo
npm run format

# Ver paquetes desactualizados
npm outdated

# Actualizar dependencias
npm update
```

## 📁 Estructura Importante

```
src/          → Código fuente (editar aquí)
├── js/       → JavaScript
└── scss/     → Estilos SCSS

dist/         → Archivos compilados (no editar)
├── main.js
└── chat-for-n8n.css
```

## 🔥 Tips

1. **Desarrollo**: Usa `npm run dev` para hot-reload
2. **Producción**: Siempre ejecuta `npm run build` antes de deployar
3. **No edites**: Nunca edites archivos en `dist/`
4. **Git**: `dist/` está en .gitignore (se genera automáticamente)

## 🐛 Problemas Comunes

```bash
# Build falla
rm -rf node_modules dist
npm install
npm run build

# Cambios no se ven
npm run build
# Luego limpia caché del navegador
```

## 📖 Documentación Completa

Ver `DEVELOPMENT.md` para documentación detallada.

